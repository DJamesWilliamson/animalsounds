# How to get started with the animalsounds package

``` r

library(animalsounds)
```

The first argument is the animal and the second it the sound it makes

``` r

animal_sounds("dog", "woof")
#> [1] "The dog goes woof!"
```
